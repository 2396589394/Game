syntax = "proto3";
package Login;

// 第三方登陆授权code
message Code
{
    string code = 1;
}

// 个人信息
message Info
{
    string token = 1;
}

service LoginRPC
{
    rpc weChat(Code) returns(Info) {}   // 微信登陆
}